This project aimed to port transformer Grouppose into MMLab's MMpose engine, to enable training and testing via MMlab's native tools. 
The main jobs carried out include conversion of checkpoints from grouppose to MMlab compatible naming. Building workflow of the model via configs, lastly, cuda compatibility configuration for later RTX models(on which it was tested on for coco val 2017).
